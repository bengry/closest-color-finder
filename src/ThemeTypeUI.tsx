import { ThemeType } from './colorPresets/_internal/createColorPreset';

export const THEME_TYPE_UI: Record<ThemeType, { label: string }> = {
  light: { label: 'Light' },
  dark: { label: 'Dark' },
};
